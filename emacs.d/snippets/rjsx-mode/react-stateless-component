# -*- mode: snippet -*-
# contributor: Gabriele Lana
# name : react-stateless-component
# key : rsc
# uuid: 46641118-4ab7-45f6-b57c-6d456ceae2f7
# --
import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {
  $2: PropTypes.string,
};

const defaultProps = {
  $2: 'Hello World',
};

const ${1:`(buffer-basename)`} = ({${2:property}}) => <div>{$2}$0</div>;

$1.propTypes = propTypes;

$1.defaultProps = defaultProps;

export default $1;
